[user]
    name = Victor Ma
    email = victor@victorma.ca
[alias]
    ad     = add
    ada    = add --all
    st     = status
    df     = diff
    dfc    = diff --check
    dfcm   = diff --check master
    dfh    = diff
    com    = co master
    ct     = commit
    cta    = commit -a
    ctm    = commit -m
    ctam   = commit --amend
    ctama  = commit -a --amend
    loga   = log --all
    lg     = log --oneline
    lga    = log --oneline --all
    lgall  = log --oneline --all
    lgr    = log --graph --oneline
    lgrall = log --graph --all --oneline
    ps     = push
    psf    = push --force
    pl     = pull
    ft     = fetch
    fta    = fetch --all
    fp     = fetch --prune
    mg     = merge
    br     = branch
    brd    = branch --delete
    bra    = branch --all
    brall  = branch --all
    rt     = remote
    rta    = remote add
    rtv    = remote -v
    co     = checkout
    cop    = checkout -
    cob    = checkout -b
    co1    = co HEAD~1
    co2    = co HEAD~2
    co3    = co HEAD~3
    co4    = co HEAD~4
    co5    = co HEAD~5
    ma     = master
    rb     = rebase
    rbi    = rebase -i
    rbm    = rebase master
    s      = stash
    sl     = stash list
    sp     = stash pop
    rbim   = rebase -i master
    rs     = reset
    rsh    = reset --hard
    cl     = clean -f
    cfd    = clean -fd
    df1    = diff HEAD~1
    df2    = diff HEAD~2
    df3    = diff HEAD~3
    df4    = diff HEAD~4
    df5    = diff HEAD~5
    plu    = pull upstream master
    dfm    = diff master
    brm    = branch --move
    bs     = bisect start
    bb     = bisect bad
    bg     = bisect good
    bl     = blame -s
    brs    = bisect reset
[fetch]
    prune = true
[advice]
    detachedHead = false
[push]
    autoSetupRemote = true
[pager]
    branch = false
[core]
    editor = vim
    excludesfile = /home/victor/.gitignore
